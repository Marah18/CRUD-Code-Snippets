<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add snippet</title>
    <link rel="stylesheet" href="/style.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/prism/1.27.0/themes/prism.min.css" rel="stylesheet" />
    <script src="https://cdnjs.cloudflare.com/ajax/libs/prism/1.27.0/prism.min.js"></script>
</head>

<body>

    <%- include('Header') %>
        <div class="snippet-list-container">
            <h2 class="snippet-tit">Your Previous Snippets</h2>
            <% if (snippets.length> 0) { %>
                <ul>
                    <% snippets.forEach(snippet=> { %>
                        <li class="snippet-item">
                            <h3>
                                <%= snippet.title %>
                            </h3>
                            <p>
                            <pre
                            class="snippet-content"><code class="language-<%='javascript' %>"><%= snippet.content %></code></pre>
                        </p>
                            <div class="editBtns">
                                <a href="/editSnippet/<%= snippet._id %>" class="edit-link">Edit</a>

                                <form action="/deleteSnippet/<%= snippet._id %>" method="POST"
                                    onsubmit="return confirm('Are you sure you want to delete this snippet?');"
                                    class="snippet-delete-form">
                                    <button type="submit" class="delete-btn">Delete</button>
                                </form>

                            </div>
                        </li>
                        <% }) %>
                </ul>
                <% } else { %>
                    <p class="no-snippets">You haven't added any snippets yet.</p>
                    <% } %>
        </div>
</body>

</html>